// 统一导入导出, 可以一次性的导出所有的 controller
import { DetailController } from './detailController'
import { HomeController } from './homeController'
import { ListController, removeListController, addMobileListController } from './listController'

export {
	  DetailController,
	  HomeController,
	  ListController,
	  removeListController,
	  addMobileListController
}